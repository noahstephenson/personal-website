services:
  - type: web
    name: noah-portfolio
    env: static
    buildCommand: pip install -r requirements.txt && pelican content -o output -s pelicanconf.py
    staticPublishPath: output
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
